# TODO - Fix total() function in pay.component.ts

## Changes Required:

- [x] 1. Add `totalAmount: number = 0;` property
- [x] 2. Modify `loadPedido()` to calculate `totalAmount`
- [x] 3. Make `total()` function safe with null check
- [x] 4. Update HTML template to use `totalAmount` instead of `total()`
- [x] 5. Remove `this.total()` call from `ngOnInit()`

## Status: Completed ✓

