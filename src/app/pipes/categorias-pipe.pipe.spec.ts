import { CategoriasPipePipe } from './categorias-pipe.pipe';

describe('CategoriasPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CategoriasPipePipe();
    expect(pipe).toBeTruthy();
  });
});
